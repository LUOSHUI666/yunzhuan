import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.luoshui.yunzhuan',
  appName: '云篆',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
